package scala.meta
package trees

private[scala] trait Api {
  // NOTE: We don't expose any methods inside scala.meta.trees
  // as part of this package's public API that will show up in scala.meta.
}

private[scala] trait Aliases {
  // NOTE: We don't expose any definitions inside scala.meta.trees
  // as part of this package's public API that will show up in scala.meta.
}
